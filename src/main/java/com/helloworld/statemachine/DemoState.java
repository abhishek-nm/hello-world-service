package com.helloworld.statemachine;

/**
 * Example states for the demo workflow. Replace with your domain states when building a real state machine.
 */
public enum DemoState {
    DRAFT,
    SUBMITTED,
    IN_REVIEW,
    APPROVED,
    REJECTED
}
